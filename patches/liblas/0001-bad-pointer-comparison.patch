*** libLAS-1.8.0/src/gt_citation.cpp	2014-08-01 15:48:16.000000000 -0700
--- libLAS-1.8.0_new/src/gt_citation.cpp	2018-07-12 16:25:38.000000000 -0700
***************
*** 155,161 ****
          if(p1[0] == '\0' || p1[0] == '\n' || p1[0] == ' ')
            p1 --;
          p2 = p1 - 1;
!         while( p2>0 && (p2[0] == ' ' || p2[0] == '\0' || p2[0] == '\n') )
            p2--;
          if(p2 != p1 - 1)
            p1 = p2;
--- 155,161 ----
          if(p1[0] == '\0' || p1[0] == '\n' || p1[0] == ' ')
            p1 --;
          p2 = p1 - 1;
!         while( p2 != 0 && (p2[0] == ' ' || p2[0] == '\0' || p2[0] == '\n') )
            p2--;
          if(p2 != p1 - 1)
            p1 = p2;
***************
*** 198,204 ****
          if(p1[0] == '\0' || p1[0] == '\n' || p1[0] == ' ')
            p1 --;
          char* p2 = p1 - 1;
!         while( p2>0 && (p2[0] == ' ' || p2[0] == '\0' || p2[0] == '\n') )
            p2--;
          if(p2 != p1 - 1)
            p1 = p2;
--- 198,204 ----
          if(p1[0] == '\0' || p1[0] == '\n' || p1[0] == ' ')
            p1 --;
          char* p2 = p1 - 1;
!         while( p2 != 0 && (p2[0] == ' ' || p2[0] == '\0' || p2[0] == '\n') )
            p2--;
          if(p2 != p1 - 1)
            p1 = p2;
